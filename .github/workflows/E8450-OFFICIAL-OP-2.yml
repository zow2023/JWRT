name: E8450-OFFICIAL-OP

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      branch:
        description: "OpenWrt Branch"
        required: true
        default: 'v25.12.4'

# 取消重复排队的旧任务，避免并发冲突与资源浪费
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REPO_URL: https://github.com/openwrt/openwrt
  REPO_BRANCH: ${{ github.event.inputs.branch || 'v25.12.4' }}
  FEEDS_CONF: E8450/feeds.conf.default
  CONFIG_FILE: E8450/25125official.config
  DIY_P1_SH: E8450/diy-part-1.sh
  DIY_P2_SH: E8450/diy-part-2.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: false
  TZ: Asia/Shanghai
  DEBIAN_FRONTEND: noninteractive

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    permissions:
      contents: write
      actions: write

    steps:
      # 1. 拉取仓库配置
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 精简版快速环境初始化
      - name: Initialization Environment
        run: |
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install -y --no-install-recommends \
            asciidoc autoconf automake autopoint binutils build-essential bzip2 \
            clang device-tree-compiler dwarves flex gawk gcc-multilib gettext git \
            libglib2.0-dev libncurses5-dev libssl-dev libtool libz-dev jq ccache ninja-build \
            libc6-dev-i386 llvm msmtp npm p7zip p7zip-full patch python3-dev \
            python3-pyelftools qemu-utils subversion texinfo tzdata uglifyjs \
            unzip upx xmlto zlib1g-dev mergerfs rename
          sudo npm install -g pnpm
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "$TZ"

          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      # 3. 极速克隆源码
      - name: Clone OpenWrt source
        working-directory: /workdir
        run: |
          git clone --depth=1 --filter=blob:none -b $REPO_BRANCH $REPO_URL openwrt
          ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

      # 4. 高效缓存恢复 (包含 Toolchain、ccache 与 dl)
      - name: Cache OpenWrt Build
        uses: klever1988/cachewrtbuild@main
        with:
          ccache: true
          toolchain: true
          skip: true
          prefix: /workdir/openwrt
          mixkey: openwrt-e8450-v25.12

      # 5. 加载 E8450 自定义 Feeds
      - name: Load custom feeds
        run: |
          [ -e "$FEEDS_CONF" ] && mv "$FEEDS_CONF" openwrt/feeds.conf.default
          chmod +x "$DIY_P1_SH"
          cd openwrt
          "$GITHUB_WORKSPACE/$DIY_P1_SH"

      # 6. 更新 & 安装 Feeds
      - name: Update & Install feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # 7. 替换 Rust feed（修复 CI LLVM 构建问题）
      - name: Replace Rust feed with guaranteed CI LLVM
        run: |
          cd /workdir/openwrt
          rm -rf feeds/packages/lang/rust
          git clone --depth 1 https://github.com/sbwml/packages_lang_rust.git feeds/packages/lang/rust
          ./scripts/feeds install -f rust

      # 8. 加载 E8450 配置文件 (.config & diy-part2.sh)
      - name: Load custom configuration
        run: |
          [ -e files ] && mv files openwrt/files
          [ -e "$CONFIG_FILE" ] && mv "$CONFIG_FILE" openwrt/.config
          chmod +x "$DIY_P2_SH"
          cd openwrt
          "$GITHUB_WORKSPACE/$DIY_P2_SH"

      # 9. 补丁：开启 MediaTek 内核 eBPF / NETKIT 选项
      - name: Patch Kernel Config
        run: |
          cd openwrt
          for cfg in $(find target/linux/mediatek -name "config-6*"); do
            echo "Checking $cfg"
            sed -i '/CONFIG_NET_SCH_BPF/d' "$cfg"
            echo "CONFIG_NET_SCH_BPF=y" >> "$cfg"
            sed -i '/CONFIG_BPF_SYSCALL/d' "$cfg"
            echo "CONFIG_BPF_SYSCALL=y" >> "$cfg"
            sed -i '/CONFIG_BPF_JIT/d' "$cfg"
            echo "CONFIG_BPF_JIT=y" >> "$cfg"
            sed -i '/CONFIG_NETKIT/d' "$cfg"
            echo "CONFIG_NETKIT=y" >> "$cfg"
            sed -i '/CONFIG_NF_CONNTRACK_CHAIN_EVENTS/d' "$cfg"
            echo "CONFIG_NF_CONNTRACK_CHAIN_EVENTS=y" >> "$cfg"
          done

      # 10. 预下载依赖包 (多线程下载 + 自动清理破坏包)
      - name: Download packages
        run: |
          cd openwrt
          make defconfig
          mkdir -p dl
          find dl -size -1024c -exec rm -f {} \;
          make download -j8
          find dl -size -1024c -exec rm -f {} \;

      # 11. 高效并发编译
      - name: Compile firmware
        id: compile
        run: |
          set -o pipefail
          cd openwrt
          
          mkdir -p ~/.ccache
          ccache -M 5G
          ccache -z

          export FORCE_UNSAFE_CONFIGURE=1
          export MAKEFLAGS="-j$(($(nproc)+1))"

          make -j$(($(nproc)+1)) || make -j1 V=s
          
          echo "status=success" >> $GITHUB_OUTPUT
          
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
          [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

          echo "===== ccache 编译统计 ====="
          ccache -s

      # 12. 整理固件产物
      - name: Organize files
        id: organize
        if: steps.compile.outputs.status == 'success' && env.UPLOAD_FIRMWARE == 'true'
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      # 13. 上传固件至 Artifacts
      - name: Upload firmware directory
        uses: actions/upload-artifact@v4
        if: steps.organize.outputs.status == 'success' && !cancelled()
        with:
          name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE }}

      # 14. 编译失败时自动保存日志
      - name: Upload compile logs on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: openwrt-build-logs
          path: |
            openwrt/logs/
            openwrt/.config

      # 16. 清理旧 Workflow 运行记录
      - name: Cleanup workflow runs
        uses: GitRML/delete-workflow-runs@main
        with:
          retain_days: 1
          keep_minimum_runs: 3
